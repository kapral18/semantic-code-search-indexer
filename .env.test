# Test environment configuration
# This file is loaded by dotenv during tests (NODE_ENV=test)
# It prevents loading the user's .env file which might interfere with tests

# Disable OpenTelemetry logging in tests
OTEL_LOGGING_ENABLED=false

# Use localhost for Elasticsearch in tests
ELASTICSEARCH_ENDPOINT=http://localhost:9200
