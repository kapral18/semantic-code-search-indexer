// src/languages/templates/custom-parser-template.ts
// This template is used for languages that use custom parsers (not tree-sitter)
// Replace {{LANGUAGE_NAME}} with your language name (lowercase)

import { LanguageConfiguration } from '../utils/parser';

/**
 * Configuration for {{LANGUAGE_NAME}} language parsing
 * 
 * This configuration uses a custom parser (parser: null).
 * Custom parsers are useful for:
 * - Markup languages (YAML, JSON, etc.)
 * - Template languages
 * - Languages without tree-sitter support
 * 
 * When parser is null, the file is parsed as a whole document or using
 * the default line-based chunking strategy.
 */
export const {{LANGUAGE_NAME}}Config: LanguageConfiguration = {
  name: '{{LANGUAGE_NAME}}',
  fileSuffixes: [{{FILE_EXTENSIONS}}],
  parser: null, // Indicates a custom parser should be used
  queries: [],  // No tree-sitter queries for custom parsers
};
