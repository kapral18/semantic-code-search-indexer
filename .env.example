# Code Indexer Environment Variables

# --- Elasticsearch Configuration ---
# Choose one of the following authentication methods.

# Option 1: For Elastic Cloud (recommended)
# Your Elastic Cloud ID.
ELASTICSEARCH_CLOUD_ID=
# A Base64 encoded API key for Elastic Cloud.
ELASTICSEARCH_API_KEY=

# Option 2: For self-managed clusters
# The endpoint of your self-managed Elasticsearch instance.
# ELASTICSEARCH_ENDPOINT=http://localhost:9200
# ELASTICSEARCH_USER=elastic
# ELASTICSEARCH_PASSWORD=changeme

# --- Indexing Configuration ---
# The name of the Elasticsearch index to store code chunks.
ELASTICSEARCH_INDEX=code-chunks

# The number of code chunks to send to Elasticsearch in a single bulk request.
BATCH_SIZE=500

# The number of CPU cores to use for parallel file parsing.
# Defaults to half the available cores if not set.
# CPU_CORES=4