# Elasticsearch configuration
# Elasticsearch Configuration
# Option 1: For Elastic Cloud (recommended)
# ELASTICSEARCH_CLOUD_ID=
# ELASTICSEARCH_API_KEY=

# Option 2: For self-managed clusters
ELASTICSEARCH_ENDPOINT=http://localhost:9200
# ELASTICSEARCH_USER=elastic
# ELASTICSEARCH_PASSWORD=changeme

# The ELSER model to use for semantic search.
# This model must be downloaded and deployed in your Elasticsearch cluster.
ELSER_MODEL=.elser_model_v2

# --- Webhook Configuration ---
# A strong, randomly generated secret for verifying GitHub webhook payloads.
GITHUB_WEBHOOK_SECRET=
# The GCP Pub/Sub topic the webhook server will publish to.
GCP_PUBSUB_TOPIC=github-pushes
# The GCP Pub/Sub subscription the VM worker will listen to.
GCP_PUBSUB_SUBSCRIPTION=github-pushes-sub-vm


# Alternatively, use an API key
# ELASTICSEARCH_API_KEY=your_api_key

# Indexing configuration
BATCH_SIZE=500
MAX_QUEUE_SIZE=1000
CPU_CORES=4
